---
import Base from '@/layouts/Base.astro';
import Nav from '@/components/Nav.astro';
import Showcase from '@/components/Showcase.astro';
import { site } from '@/data/site';
import { laneSwitch } from '@/lib/transitions';

const locale = 'es';
const title = `${site.fullName} - Clases, conciertos y contacto`;
const description = 'Clases privadas de guitarra clasica, conciertos y contacto directo con Eulogio Albalat.';
const pageURL = new URL('/classes/', Astro.site);
const email = site.socials.find((social) => social.href.startsWith('mailto:'))?.href.replace('mailto:', '') ?? 'hello@eulogioalbalat.com';
const jsonLd = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'ContactPage',
      '@id': `${pageURL}#webpage`,
      url: pageURL.toString(),
      name: title,
      description,
      inLanguage: 'es',
      breadcrumb: { '@id': `${pageURL}#breadcrumb` },
      about: { '@type': 'Person', name: site.fullName, email },
    },
    {
      '@type': 'BreadcrumbList',
      '@id': `${pageURL}#breadcrumb`,
      itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Inicio', item: new URL('/', Astro.site).toString() },
        { '@type': 'ListItem', position: 2, name: 'Clases y conciertos', item: pageURL.toString() },
      ],
    },
    {
      '@type': 'Course',
      '@id': `${pageURL}#private-classes`,
      name: 'Clases privadas de guitarra clasica',
      description: 'Clases privadas de guitarra clasica adaptadas al nivel, repertorio y objetivos de cada alumno.',
      provider: { '@type': 'Person', name: site.fullName },
      inLanguage: 'es',
    },
    {
      '@type': 'Service',
      '@id': `${pageURL}#concert-booking`,
      name: 'Conciertos de guitarra clasica',
      description: 'Consultas para recitales, conciertos privados y propuestas musicales.',
      provider: { '@type': 'Person', name: site.fullName },
      serviceType: 'Classical guitar performance',
      inLanguage: 'es',
    },
  ],
};
---
<Base locale={locale} title={title} description={description} jsonLd={jsonLd}>
  <Nav locale="es" />
  <main id="main-content" transition:name="page-lane" transition:animate={laneSwitch}>
    <Showcase locale="es" initialDetail="classes" />
  </main>
</Base>
