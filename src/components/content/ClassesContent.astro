---
import { site } from '@/data/site';
import type { Locale } from '@/i18n';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;

const profileLinks = site.socials.filter((social) => social.href.startsWith('http'));
const email = site.socials.find((social) => social.href.startsWith('mailto:'))?.href.replace('mailto:', '') ?? 'hello@eulogioalbalat.com';
const mailCharCount = email.length;

const copy = {
  es: {
    eyebrow: 'Masterclass · conciertos · contacto',
    heading: 'Abrir una conversación musical.',
    intro: 'Para masterclass, recitales o propuestas de concierto, lo más útil es empezar por el contexto: nivel, repertorio, fecha, lugar y tipo de escucha.',
    classTitle: 'Masterclass',
    classDescription: 'Técnica, sonido, lectura y repertorio con una dirección de estudio clara, tras más de tres décadas de cátedra en el Conservatorio Superior de A Coruña.',
    concertTitle: 'Propuestas de concierto',
    concertDescription: 'Un abanico amplio de programas listos para llevar al escenario, desde J. S. Bach hasta el siglo XX, para ciclos, espacios culturales y eventos privados.',
    repertoireTitle: 'Repertorio español',
    repertoireDescription: 'Especializado en el repertorio español, los compositores de las generaciones del 98 y del 27 son piedra de toque habitual en sus programas: Albéniz, Granados, Falla, Turina, Torroba, Rodrigo, Llobet, Pujol y ambos Sainz de la Maza, por citar algunos.',
  },
  en: {
    eyebrow: 'Masterclass · concerts · contact',
    heading: 'Start a musical conversation.',
    intro: 'For masterclasses, recitals, or concert proposals, the best starting point is context: level, repertoire, date, place, and kind of listening.',
    classTitle: 'Masterclass',
    classDescription: 'Technique, sound, reading, and repertoire with a clear direction for study, backed by more than three decades as a conservatory professor in A Coruña.',
    concertTitle: 'Concert proposals',
    concertDescription: 'A wide range of concert-ready programs, from J. S. Bach to the twentieth century, for series, cultural venues, and private events.',
    repertoireTitle: 'Spanish repertoire',
    repertoireDescription: 'A specialist in Spanish repertoire: the composers of the generations of 1898 and 1927 — Albeniz, Granados, Falla, Turina, Torroba, Rodrigo, Llobet, Pujol, and both Sainz de la Maza, to name a few — are a regular touchstone of his programs.',
  },
} as const;

const t = copy[locale];
---
<section class="detail-section classes-detail relative overflow-hidden">
  <div class="pointer-events-none absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-accent/60 to-transparent"></div>
  <div class="pointer-events-none absolute right-[-12vw] top-28 h-[38rem] w-[38rem] rounded-full border border-accent/20"></div>
  <div class="pointer-events-none absolute right-[-5vw] top-48 h-[24rem] w-[24rem] rounded-full border border-ink/10"></div>

  <div class="mx-auto grid max-w-7xl grid-cols-12 gap-y-8 md:gap-y-10 gap-x-0 lg:gap-x-16 xl:gap-x-20">
    <div class="col-span-12 min-w-0 lg:col-span-9">
      <div class="detail-hero detail-copy-inset">
        <p data-reveal="label" class="detail-kicker">{t.eyebrow}</p>
        <h1 data-reveal="heading" class="contact-heading mt-5 md:mt-7 max-w-3xl font-display font-light leading-[0.9] text-ink">
          {t.heading}
        </h1>

        <a
          data-reveal="body"
          href={`mailto:${email}`}
          class="contact-email mt-6 md:mt-10 block max-w-full font-display leading-none text-ink underline decoration-1 underline-offset-[0.16em] transition-colors duration-200 hover:text-accent focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent"
          style={`--mail-chars: ${mailCharCount};`}
        >
          {email}
        </a>

        <p data-reveal="body" class="mt-6 md:mt-9 max-w-xl text-lg leading-7 text-ink-muted md:text-xl md:leading-8">
          {t.intro}
        </p>
      </div>

      <div class="mt-8 md:mt-12 grid gap-3 md:gap-4 sm:grid-cols-2">
        <article data-reveal="body" class="relative min-h-44 overflow-hidden border border-ink/10 p-5">
          <span class="font-mono text-xs uppercase tracking-[0.2em] text-accent">01</span>
          <h2 class="mt-7 font-display text-4xl font-light text-ink">{t.classTitle}</h2>
          <p class="mt-4 text-sm leading-6 text-ink-muted">{t.classDescription}</p>
        </article>
        <article data-reveal="body" class="relative min-h-44 overflow-hidden border border-ink/10 p-5">
          <span class="font-mono text-xs uppercase tracking-[0.2em] text-accent">02</span>
          <h2 class="mt-7 font-display text-4xl font-light text-ink">{t.concertTitle}</h2>
          <p class="mt-4 text-sm leading-6 text-ink-muted">{t.concertDescription}</p>
        </article>
        <article data-reveal="body" class="relative overflow-hidden border border-ink/10 p-5 sm:col-span-2">
          <span class="font-mono text-xs uppercase tracking-[0.2em] text-accent">03</span>
          <h2 class="mt-7 font-display text-4xl font-light text-ink">{t.repertoireTitle}</h2>
          <p class="mt-4 max-w-3xl text-sm leading-6 text-ink-muted">{t.repertoireDescription}</p>
        </article>
      </div>

      {profileLinks.length > 0 && (
        <ul data-reveal="cta" class="detail-copy-inset mt-6 md:mt-10 flex flex-wrap gap-3 md:gap-4">
          {profileLinks.map((social) => (
            <li>
              <a class="inline-flex min-h-11 items-center text-sm font-semibold uppercase tracking-[0.16em] text-ink-muted transition-colors hover:text-ink" href={social.href} target="_blank" rel="noreferrer">
                {social.label} ->
              </a>
            </li>
          ))}
        </ul>
      )}
    </div>

  </div>
</section>

<style>
  .contact-email {
    overflow-wrap: anywhere;
    font-size: clamp(1.35rem, 6.2vw, 3.4rem);
  }

  .contact-heading {
    overflow-wrap: anywhere;
    font-size: clamp(3.5rem, 15vw, 5.2rem);
    text-wrap: balance;
  }

  @media (max-width: 767px) {
    .classes-detail :global(article) {
      min-height: 0;
      padding: var(--detail-surface-padding);
    }

    .classes-detail :global(article h2) {
      margin-top: 1.25rem;
      font-size: 2rem;
    }

    .classes-detail :global(article p) {
      margin-top: 0.75rem;
    }
  }

  @media (min-width: 1024px) {
    .contact-email {
      font-size: min(3.2rem, calc(100% / (var(--mail-chars) * 0.58)));
    }

    .contact-heading {
      font-size: clamp(5rem, 6.2vw, 6.8rem);
    }
  }
</style>
