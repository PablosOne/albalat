# Albalat Implementation Progress Ledger

Plan: docs/superpowers/plans/2026-07-04-albalat-website.md
Branch: phase-0-foundation

## Phase 0 - Foundation
- Task 0.1: complete
- Task 0.2: complete
- Task 0.3: complete
- Task 0.4: complete
- Task 0.5: complete
- Task 0.6: deferred

## Phase 1 - The Stage + About
- Task 1.1: complete
- Task 1.2: complete
- Task 1.3: complete
- Task 1.4: complete
- Task 1.5: complete
- Task 1.6: complete

## Log
- Task 0.1: complete (commits 5a6767c..a562d20, review clean)
- Task 0.2: complete (commits dc5910c..13c07a5, review clean)
- Task 0.3: complete (commits 978d842..919934c, review clean)
- Task 0.4: complete (commits 48288a3..35696ac, review clean)
- Task 0.5: complete (commits 6c797d6..b2e9425, review clean)
- Task 0.6: deferred - needs user's GitHub push consent + Cloudflare Pages dashboard (their account). Proceeding to Phase 1; revisit before launch.
- Task 1.1: complete (commits a97787f..d15cb2d, review clean)
- Task 1.2: complete (commits c96e189..efbbb5d, review clean)
- Task 1.3: complete (home stage assembled for es/en; checks/build/tests/browser smoke pass)
- Task 1.4: complete (signature string + spotlight animation; reduced-motion browser smoke pass)
- Task 1.5: complete (detail layout, About pages, lane-switch transition, YouTube/Spotify links wired)
- Task 1.6: complete (Playwright smoke tests added; e2e green)

# Persistent Music Player Progress Ledger

Plan: docs/superpowers/plans/2026-07-05-persistent-music-player.md
Branch: phase-0-foundation
Baseline commit: 1793a27 (WIP checkpoint of unrelated pre-existing work)

## Tasks
- Task 1: complete
- Task 2: complete
- Task 3: complete
- Task 4: complete
- Task 5: complete
- Task 6: complete
- Task 7: complete

## Log
- Task 1: complete (commits ae27196..2b6d609, review clean; minor cosmetic finding: Muneira/Muñeira accent typo, plan-mandated, no fix needed)
- Task 2: complete (commits 2b6d609..14980c9, review clean; original implementer hit API rate limit mid-task, controller verified+committed its correct fix; minor: unused-ish type alias name, redundant regex branch, both noted not fixed)
- Task 3: complete (commit 98bcbe4; landed directly by a concurrent session without going through this review process — reviewed retroactively by the cross-scroll controller on 2026-07-05, review clean, minor notes: audio.play() lacks .catch(), a couple of edge cases untested per the brief's own spec)
- Task 4: complete (commits 1caf123, 981ed0d, 1a754ad; review round 1 found 2 Important issues — no prefers-reduced-motion gating (fixed correctly first try), and stale locale copy after a client-side ClientRouter navigation. The first locale fix (981ed0d) looked plausible but was proven wrong by round-2 review actually reading Astro's swap-functions.js source: transition:persist means the persisted DOM node's own data-copy attribute is never refreshed by Astro's swap, so re-running the refresh function on every page-load just reapplied the same stale value. Round-3 fix (1a754ad) correctly sources locale from document.documentElement.lang (which Astro's swapRootAttributes DOES keep fresh) and embeds the full bilingual dict as a script-scope constant instead of a persisted-node attribute; independently verified against Astro's source and built HTML output, re-reviewed clean. Minor noted: bilingual copy dict now duplicated between Astro frontmatter and client script — not blocking.)
- Task 5: complete (commit ba7ce57, review found 1 Important — engine.subscribe() in AlbumStoryPlayer.astro's playback wiring never captured/called the returned unsubscribe, so each client-side ClientRouter navigation back to /music leaked another permanent subscriber in the window-singleton nowPlaying engine, pinning detached DOM and doing wasted per-timeupdate-tick work on stale button arrays forever. Inherited from the plan brief's own snippet, not an implementer deviation. Fixed in commit da797e1: module-scoped unsubscribeNowPlaying captures the previous unsubscribe and calls it before re-subscribing on each init. Build/check/test all green after fix. 2 Minor notes not fixed: redundant dataset writes on every emit tick, first-paint scroll-theme tint waits on two dynamic imports resolving — both cosmetic.)
- Task 6: complete (commit 358a805, appends theme defaults + `[data-album-story]::before` ambient gradient layer + reduced-motion-gated `album-drift` keyframe to global.css, exactly per brief; the dead `:root { transition: none; }` line from the plan's literal text was deliberately skipped as instructed. COMMIT-SCOPING INCIDENT during this task: global.css had ~277 lines of another concurrent session's unrelated uncommitted WIP (font-face, root vars, mobile media queries, gfdu-mobile-* styles) sitting in the shared working tree; the implementer's `git commit -m "..." -- src/styles/global.css` swept all of it into one commit (2e6df40) because pathspec-scoped commit re-adds the CURRENT WORKING TREE content of that path, not just what was staged — this is a previously-undocumented gotcha with this session's own "commit -- path" convention when the SAME file has mixed WIP from multiple sessions (as opposed to the convention working fine when one task owns a whole file). Controller split it: `git reset --soft HEAD~1` (safe, commit was local/unpushed) + hand-built a minimal patch containing only the isolated 32-line append (verified via `git diff <old>..<bad-commit>` showing it as a clean trailing hunk with no interleaving) + `git apply --cached` to stage only that. A second scoping issue then surfaced: the shared git INDEX already had ANOTHER concurrent session's guitar-removal-related files (Station.astro, GuitarNotes.astro deletion, i18n JSON, site.test.ts, this ledger file) pre-staged from before this conversation even began, and a plain `git commit -m "..."` (no pathspec) swept those into a second bad commit (708e4a3) too, since committing with no pathspec commits the WHOLE index. Fixed by: `git reset --soft HEAD~1` again, then `git reset HEAD -- <those other paths>` (index-only, un-stages without touching their working-tree file content at all) before the final clean commit (358a805, 1 file/32 lines). Those other-session files are now unstaged-but-content-identical (nothing lost); re-staging them programmatically was blocked by the permission system as "modifying shared resources outside task scope" — left for their owning session to `git add` back when ready. Lesson for future tasks touching a file with known cross-session WIP: verify `git diff --cached --name-only` is EXACTLY the intended path set immediately before every commit, especially after any `git reset`.)
- FINAL WHOLE-BRANCH REVIEW (report at .superpowers/sdd/mp-FINAL-review-report.md): 1 Critical, fixed in commit 3bf173c. Critical (C1): the cross-nav persistence e2e test (Task 7, written just before this review) clicked the "Videos" nav link and asserted a URL change to /videos — but on this branch /music renders via Showcase+lanes, and Nav's "Videos" link is now a same-page #hash lane trigger intercepted by lanes.ts (no ClientRouter document swap at all, URL becomes /music#videos). This means the test would fail on a real run (never executed live this session, only tsc-checked, which can't catch a runtime-navigation-semantics issue), AND even ignoring the failing assertion, transition:persist -- the actual feature under test -- went completely unexercised, so a real regression there would have passed CI. Fixed by retargeting the click at the Home link (href="/", the only real cross-document route reachable from /music, genuinely not intercepted by lanes.ts) and asserting the URL becomes "/" instead. Also fixed in the same commit, a Minor (M1) the review flagged as now-relevant rather than theoretical: audio.play() in nowPlaying.ts (both call sites) lacked .catch(), harmless while unwired but a real unhandled-rejection risk now that Task 5 wires up real clicks including the un-gestured auto-advance-on-'ended' path. Minors NOT fixed (cosmetic/negligible): applyTheme's redundant re-writes on every timeupdate tick (NowPlayingBar-side counterpart to the already-noted AlbumStoryPlayer one), a stale AlbumStoryPlayer subscription lingering harmlessly while navigated away from /music, --album-* CSS vars persisting globally on :root (desirable, not a bug). Everything else (type consistency across all 5 files, now-playing-wins theme priority in every path, close-reverts-to-scroll-theme, reduced-motion belt-and-suspenders, Nocturno-at-index-5 ordering) verified correct. Build/check/test green after fix. Persistent Music Player plan (Tasks 1-7 + final review) is now complete.
- Task 7: complete (commit fd30d12; tests/e2e/music.spec.ts fully rewritten per the plan's spec — the old suite tested the boxed Spotify facade removed by Task 5. Covers rendering, track-click raising the persistent bar, cross-nav audio/bar persistence, expand-to-full, close, ambient-theme CSS var reacting to the playing track, and localized /en/music copy. One correction to the plan's literal test text: the persistence test's nav-link selector was changed from a case-insensitive /Videos/i regex to the literal accented Spanish text 'Vídeos', since the regex (no í) would not have matched the real rendered label. NOT executed via a live browser this session (standing instruction) — verified via a standalone `tsc --noEmit` pass (matching the same check applied to every other e2e spec for a clean-baseline comparison) plus manual cross-reference of every selector/global against the real component contracts (AlbumStoryPlayer.astro, NowPlayingBar.astro, nowPlaying.ts, trackTheme.ts, Nav.astro, i18n copy, discography.ts track ordering for the index-5-is-Nocturno assumption). Persistent Music Player plan (Tasks 1-7) is now functionally complete — final whole-branch review still pending.)

# Cross-Scroll Navigation Progress Ledger

Plan: docs/superpowers/plans/2026-07-05-cross-scroll-navigation.md
Branch: phase-0-foundation
Baseline commit: 14980c9

Note: the Persistent Music Player plan (above) also modifies Base.astro,
global.css, music.spec.ts, and AlbumStoryPlayer.astro. Re-check its ledger
before this plan's tasks that touch those files (5.2, 9.x, 10.2) — resolve
any new overlap with the user before proceeding.

## Tasks
- Task 0.1: complete
- Task 0.2: complete
- Task 1.1: complete
- Task 1.2: complete
- Task 2.1: complete
- Task 3.1: complete
- Task 3.2: complete
- Task 4.1: complete
- Task 4.2: complete
- Task 5.1: complete
- Task 5.2: complete
- Task 6.1: complete
- Task 6.2: complete
- Task 7.1: complete
- Task 7.2: complete
- Task 8.1: complete
- Task 9.1: complete
- Task 9.2: complete
- Task 9.3: complete
- Task 10.1: pending
- Task 10.2: pending
- Task 10.3: pending

## Log
- Task 0.1: complete (commit 4776092, review: 1 Critical finding resolved as false-positive by controller — Step 2's package.json verification is a read-only check with no diff artifact; independently confirmed correct against package.json and the implementer's report)
- Task 0.2: complete (commit 8858bd4, review clean)
- Task 1.1: complete (commits a5a0e92..67691fb, review clean; required a follow-up commit for an uncommitted jsdom devDependency the implementer had installed locally but not committed; unrelated dirty files from other in-progress sessions in the working tree were left untouched throughout)
- Task 1.2: complete (commit 2e403eb, review clean; minor noted behavior nuance — strings now animate synchronously on initSignature() call rather than waiting for gsap's dynamic import, per the brief's own prescribed rewrite)
- Task 2.1: complete (commit 3682c73, review clean)
- BRANCH RENAME: phase-0-foundation was renamed to main mid-session (reflog confirms rename, not divergence — same continuous history). Commit dd573dd ("docs: amend cross-scroll plan...") accidentally bundled another concurrent session's staged classes/contact-merge work (site.ts, classes.astro x2 locales, contact.astro deletions x2, InquiryForm.astro, seo.spec.ts, site.test.ts, astro.config.mjs) due to a shared working-tree index race between `git add <my-file>` and `git commit`. User decided: leave as one commit, no history rewrite. Going forward, use `git commit -- <exact-path>` (pathspec-scoped) instead of `git add` + plain `git commit` to prevent recurrence.
- STATION MERGE: a concurrent session merged the standalone Contact station into Classes (site.ts drops 'contact'; classes.astro redesigned to embed contact form/email/socials; contact.astro + en/contact.astro deleted). User confirmed intentional/final. Plan doc docs/superpowers/plans/2026-07-05-cross-scroll-navigation.md amended in place (Tasks 3.1, 4.1, 4.2, 6.1, 7.1, 10.1, 10.2) to drop ContactContent.astro and the contact station/lane/route everywhere; 5 stations post-hero, 4 detail lanes (music/videos/guitar/classes) going forward.
- Task 3.1: complete (commit 4700ff4, review clean; GuitarNotes.astro + ClassesContent.astro extracted from current merged classes.astro, no ContactContent.astro created, route pages left untouched)
- Task 3.2: complete (commit 64df2fb, review clean)
- Task 4.1: complete (commit 946288f, review clean)
- Task 4.2: complete (commits b489d79, 054c9fe; review round 1 found 2 Important issues — classes artifact should be 'signal' not 'fretboard' (another session repointed StationTeaser.astro's map when merging contact into classes), and .station__copy was missing position/z-index/max-width vs the original .station-teaser__copy, risking the artifact painting over text. Both independently verified by controller, fixed, re-reviewed clean. Plan doc also corrected (commit 06d2534) to prevent the same drift in future briefs.)
- Task 5.1: complete (commit 384c845, review clean; first attempt hit an API rate limit mid-task after correctly writing the RED test, resumed cleanly by a second implementer instance)
- Task 5.2: complete (commits d759bab, 195989a, 171d0ee; review round 1 found 2 Important race conditions in openLane/closeLane (state.openId set too late; no guard against opening a second lane) — fixed, but the fix itself exposed a new Important issue (no gsap.killTweensOf before tweens, causing visible animation jank on rapid open/close/switch) — fixed in a third commit, re-reviewed clean. One edge case (double-Escape during in-flight close can corrupt a subsequently-opened lane's state) explicitly deferred as a known, tracked, low-likelihood follow-up — NOT fixed, revisit before final ship if e2e testing surfaces it.)
- CRITICAL FIX (commit d83db99): discovered during Task 6.1's verification — no lane could be opened by ANY click (down-arrow, Nav, HUD) because Astro's <ClientRouter/> (Base.astro <head>) registers its own bubble-phase click listener that runs before lanes.ts's, calls preventDefault() on every same-origin anchor click, and lanes.ts's onClick bailed on e.defaultPrevented before ever checking [data-lane-open]. Fixed by registering lanes.ts's click listener in the capture phase ({capture:true}, matching flag on removeEventListener) + stopPropagation() on the two fully-handled branches. Reviewed clean; reviewer independently read ClientRouter.astro's source to confirm the mechanism and confirmed no regression to modifier-clicks, unrelated anchors, or genuine cross-page navigation. One benign side-note: showcase.desktop.ts's separate revealSoon click listener no longer fires synchronously for lane clicks (still fires via its own scroll-event subscription, since openLane/closeLane call scrollToPanel) — not a defect.
- Task 6.1: complete (commit 5377217, review clean; existing e2e test "lane switch navigates to About" now fails as an expected side effect of About having no detail lane per the plan — will be rewritten in Task 10.1, not a Stage.astro regression, independently confirmed by reviewer)
- Task 6.2: complete (commit fd3587e, review clean)
- Task 7.1: complete (commit 73fedb4, review clean; all 10 routes (5 stations x 2 locales) rewritten to thin <Stage initialDetail> wrappers, SEO frontmatter preserved byte-for-byte, dead code from deleted inline bodies correctly removed, independently verified)
- Task 7.2: complete (commit 30b8e49, review clean)
- Task 8.1: complete (commit 9a6074b, review clean; commit was messily scoped by the implementer — swept in the shared ledger + 2 Playwright test-results artifacts despite pathspec instructions — cleaned up in follow-up commits d328ff7/a676ee9, test-results/ now gitignored. Reviewer flagged a ⚠️ on runtime aria-current for Nav items; controller independently confirmed initHudStatus() only targets HUD's own [data-hud-station-link], NOT Nav — Nav items get no active-state indicator at all now (SSR or runtime). Resolved as intentional per the plan's own "keep it simple" instruction, not a defect — HUD already surfaces the active station.)
- GUITAR STATION REMOVAL: a concurrent session removed the entire Guitar station (GuitarNotes.astro, guitar-notes.ts, /guitar routes x2 locales, site.ts entry, Station.astro order/artifact maps) — uncommitted/in-progress at time of discovery (during Task 9.1). User confirmed intentional/final. Plan doc amended (commit e013bba): 4 stations post-hero, 3 detail lanes (music/videos/classes) for all remaining tasks (10.1 e2e updated in place). Controller did NOT touch site.ts/Stage.astro/i18n itself — left that to the other session to finish/commit.
- Task 9.1: complete (commit c3bef02, review clean; implementer correctly split its commit from the concurrent guitar-removal WIP via git apply --cached on an extracted patch, verified clean by reviewer; used a separate .station__artifact-lean wrapper element to avoid GSAP's inline parallax-x transform clobbering the CSS-var-based lean transform on the same element)
- Task 9.2: complete (commits bda1e06, 815b872; review approved with one Important hardening suggestion (defensively clear state.detachY before reattaching, mirroring the file's own gsap.killTweensOf idiom) despite no demonstrated live leak — fixed as cheap insurance given lanes.ts's fragile race-condition history, re-reviewed clean. data-parallax-y added to DetailLane's title and a decorative background div in AlbumStoryPlayer, verified non-conflicting with other transform-writing code.)
- Task 9.3: complete (commit 2988fc3, review clean; documented actual clamp values — Station.astro uses vw not cqw as the original plan text loosely assumed, only DetailLane.astro uses cqw — documented accurately rather than papered over; added missing --font-mono var. Phase 9 (signature motion) complete.)
- MAJOR PIVOT (Task 10.1 blocked, discovered 2026-07-05): a third concurrent session replaced the ENTIRE home page architecture — index.astro/en/index.astro now render a new Showcase.astro+Panel.astro system (gfdu-styled, new Panel/getHomePanels() data model in site.ts) instead of Stage.astro. This Showcase has ZERO in-page detail lanes; clicking anything either pans the horizontal stage or fully navigates to /music,/videos,/classes (which still work via Stage). User confirmed: keep the new Showcase visual design, integrate the lane system into it so content opens in-place. Diagnosed via live Playwright investigation: lanes.ts/showcase.ts need ZERO changes (already generic, already firing correctly on Home) — only missing piece is DetailLane markup + href repointing. Added as Phase 11 in the plan doc (commit 9e98edf). Task 10.1 (e2e tests) parked mid-flight (uncommitted lanes.spec.ts/stage.spec.ts written against the OLD assumption that lanes only exist on sub-routes) — will resume once Phase 11 lands.
- Task 11.1: complete (commit d8faf35, review clean; DetailLane markup for music/videos/classes added to Showcase.astro mirroring Stage.astro's pattern, correctly adapted for Panel.heading being a plain string not Bilingual; verified via 11 Playwright checks both locales + differential test proving 5 pre-existing stage.spec.ts failures are unrelated (Home nav/HUD wiring gap from the Showcase-swap session, not this task))
- Task 11.2: complete (commit 4ae83ef, review clean; the 3 targeted href changes (music/videos/classes "Open X" links -> #hash) verified correct and isolated within a large (161+/15-) diff that unavoidably swept in two OTHER sessions' uncommitted site.ts work (guitar removal + the entire Panel/getHomePanels system) since git pathspecs can't isolate line-level changes within one shared file — reviewer explicitly instructed not to penalize that content, correctly focused only on the 3 hrefs. Phase 11 (Showcase integration) complete: home page now has working in-place lane navigation for music/videos/classes on top of the new gfdu-styled design.)
- Task 10.1: complete (commit 1c3db79, review approved; lanes.spec.ts rewritten to match the current Showcase architecture (plain #hash panel links, no data-lane-open on Home), 6/6 passing stable across repeats. Reviewer independently re-diagnosed the implementer's stage.spec.ts failure report: NOT a general "Lenis animated scrollTo is broken" issue as claimed, but a sub-frame startup race (Lenis's scroll limit not finalized in the same tick it's constructed) that only manifests on a click fired in the first ~100ms after page load — confirmed via direct testing that any real-world-timed click works correctly. Corrected diagnosis recorded here for any future follow-up; not a blocking regression.)
- USER DIRECTIVE 2026-07-05: stop running Playwright-heavy verification loops, prioritize finishing implementation. Applying: minimal/no live-browser verification for remaining tasks unless diagnosing a specific reported bug; rely on pnpm build + unit tests + code review. User reported "navbar opens separate pages" — investigated directly (one minimal Playwright check, not a suite): confirmed on current code the top Nav.astro correctly opens lanes/scrolls in-place for about/music/videos/classes, no JS errors, no extra navigation entries. Likely reflects pre-Task-11.2 state (the panel-internal "Ver musica" etc. links, not the navbar itself, were the ones actually causing full navigation) or a stale/cached page. Reported back to user; awaiting confirmation if issue persists.
- Task 10.2: complete (commit 5c0e519; music.spec.ts needed no changes, seo.spec.ts fixed (stale albalat.pages.dev domain), videos.spec.ts lane-scoping bug fixed. Implementer found and root-caused (not fixed, out of file-scope) a real bug in lanes.ts: reused data-lane-open attribute name as both a click-detection selector AND an <html>-level state marker, so once a lane is open, every click anywhere (including inside the lane) climbs to <html> via closest() and gets swallowed as a duplicate "open lane" action — breaks all interactivity inside every open lane. Immediately dispatched as its own fix task (see below), since this is core lanes.ts functionality from this plan, not out-of-scope WIP.)
- CRITICAL FIX (commit 66da7c8): renamed the <html> state-marker attribute from data-lane-open to data-open-lane (2 lines: openLane's setAttribute, closeLane's finish()'s removeAttribute) to eliminate the collision. LaneArrow.astro's own data-lane-open={targetId} trigger attribute and lanes.ts's pluckThreshold selector (both legitimate, unrelated uses of the same name) left unchanged. Implementer verified via negative control: reverted fix via stash, confirmed a synthetic in-lane button's click was swallowed; restored fix, confirmed it fires. Build/tests green. Review: approved clean, confirmed both legitimate unrelated usages (LaneArrow's trigger attribute, pluckThreshold's selector) correctly left untouched, confirmed the rename structurally eliminates the ancestor-match collision.
- Task 10.3: complete (no commit — pure verification, adjusted per user directive to skip the manual browser matrix). Build: 12 pages, 0 errors. Typecheck: 0 errors/warnings, 1 hint (unused `prefix` in site.ts, left alone — file owned by another session's uncommitted WIP). Unit: 37/37. E2E full run: 22/27 passing, the 5 failures all in stage.spec.ts, exactly matching the already-documented Home nav/HUD wiring gap from the Showcase-swap session (Task 11.1's log) — no NEW failures. Phase 10 (tests & cleanup) complete.
- FINAL WHOLE-BRANCH REVIEW (2026-07-05, base 14980c9 -> 66da7c8, scoped diff at .superpowers/sdd/xscroll-FINAL-review.diff, report at .superpowers/sdd/xscroll-FINAL-review-report.md): no Critical defects, all previously-tracked bugs confirmed fixed. 2 Important findings, both put to the user for a decision (both approved to act on):
  - I1 (design/product): home (`/`) renders the new gfdu Showcase/Panel system; the 4 SEO sub-routes (about/music/videos/classes x2 locales) still rendered the older Stage/Station design with different numbering — an incomplete Phase-11 migration, not an intended final state. USER DECISION: migrate sub-routes to Showcase now and retire Stage/Station entirely (see below, in progress).
  - I2 (real bug, confirmed by controller via direct Astro source trace, not speculative): Base.astro's core init script (`initScroll`/`initShowcase`/`initLanes`/`initSignature`) has byte-identical text across every route, so Astro's own `detectScriptExecuted` dedup (keyed by exact script text, in transitions/swap-functions.js) marks it "already ran" and `runScripts()` skips re-executing it on every subsequent ClientRouter swap — confirmed via reading astro@5.18.2's actual dist source, not guessed. Consequence: navigating back to a showcase-bearing page via a client-side link (e.g. Nav "Inicio") left the horizontal pin/scrub and lanes permanently dead until a hard reload — no e2e test covers this path (every existing test does a fresh `page.goto`, never SPA-navigates back to Home). USER DECISION: fix now, code-only verification (no live browser this session). FIXED in commits 0310418 + scroll.ts changes: `initScroll` (src/lib/scroll.ts) now returns a teardown function (kills the gsap ticker callback, unsubscribes the lenis scroll listener, calls `lenis.destroy()`, clears `window.__lenis`) instead of the raw Lenis instance — the only call site (Base.astro) never consumed the returned instance directly. Base.astro now registers `run()` on `astro:page-load` in addition to the existing immediate/DOMContentLoaded call (mirroring the idiom already used by AlbumStoryPlayer.astro/NowPlayingBar.astro), capturing and tearing down all 4 subsystems' cleanup functions before every re-init — safe against the resulting double-fire on the very first load since teardown-then-init is idempotent (same pattern the codebase already relies on elsewhere). `heroEntrance()` deliberately gated to run only ONCE per session (documentElement dataset flag, survives swaps) rather than on every astro:page-load, since it's a decorative one-time intro, not required for interactivity, and replaying a multi-second cinematic on every soft-nav back to Home would be a UX regression, not a fix. Verified via build/check/test only (0 errors, 37/37 tests) — NOT live-browser-confirmed per this session's standing instruction; flag for a real cross-route-nav check before next release if Playwright becomes available.
- I1/I2 FIX REVIEW (report at .superpowers/sdd/xscroll-i1-i2-fix-review-report.md): both fixes substantially correct (teardown coverage complete, no leaks; deleted components confirmed unreferenced; SEO frontmatter untouched). 2 Important findings, both fixed in commit 091159c: (A1) run() in Base.astro had no re-entrancy guard — on first load it fires twice (immediate call + astro:page-load, which the router also dispatches off the native window 'load' event) and if the second fire landed while the first was still suspended on initScroll/initShowcase's dynamic import(), both would complete and register teardown, leaving two Lenis instances + duplicate pinned ScrollTriggers running simultaneously; fixed with a generation token (only the call still current when its awaits resolve keeps its teardown, a superseded call tears itself down immediately). (A2) Showcase.astro's initialDetail relay (inline script setting document.documentElement.dataset.initialDetail) is itself subject to the exact same byte-identical-script dedup that motivated the I2 fix, per-route: on a REPEAT SPA visit to the same sub-route within a session (e.g. music->home->music) the script gets skipped and the detail lane silently stops auto-opening (not a regression — old Stage-based SPA auto-open never worked either — but a gap in the I2 fix's claim of full parity). Fixed with `data-astro-rerun` on the inline script (Astro's own escape hatch to opt a script out of the dedup). Also removed the heroPlayed dataset-flag guard around heroEntrance() as dead code resting on a wrong premise (documentElement attributes do NOT survive swaps, per swapRootAttributes; heroEntrance already only ran once because the enclosing script body itself is deduped and never re-executes, independent of the flag). Minor not fixed: tests/e2e/stage.spec.ts has 2 stale tests asserting the retired HUD's data-hud-station/data-hud-station-link selectors that ShowcaseHUD never rendered — pre-existing (Home already used ShowcaseHUD before this session's migration), not introduced by these commits, would fail if e2e were run. Build/check/test green after all fixes (0 errors, 37/37 tests).
- SUB-ROUTE SHOWCASE MIGRATION (I1, commit 4e0600b): all 8 sub-route pages (about/music/videos/classes x es/en) now render `<Showcase locale=... initialDetail=...>` instead of `<Stage>`. Showcase.astro gained an `initialDetail` prop (same documentElement-dataset-flag pattern Stage used, read by lanes.ts's existing `initLanes({ initialDetail })` — zero changes needed there). Stage.astro/Station.astro/HUD.astro deleted (confirmed via grep: no other importers existed). One pre-existing uncommitted diff to Stage.astro from the concurrent guitar-removal session was discarded by this deletion — verified it was only guitar-reference cleanup (unused import, a type alias rename, a getPanelScrollWeight call-signature update), fully moot once the file itself is retired, no functional work lost. Deliberately NOT done, flagged as follow-ups: (1) dead Station/HUD-only CSS classes in global.css (`.station-teaser__*`, `.hud-nav__*`, `.hud-station__*`, `.hud-progress__*`) left in place — that stylesheet region has unrelated concurrent-session WIP mixed in right now, too risky to hand-edit alongside it; harmless dead code in the meantime. (2) ShowcaseHUD.astro is markedly simpler than the old HUD.astro (just a progress bar, no per-panel nav links/current-station name) — Nav.astro's top nav already provides equivalent #hash links to every section so this is not a navigation dead-end, but it is a real reduction in secondary/floating-overlay affordance on what are now all routes, not just Home; not fixed, since it's a pre-existing characteristic of the already-shipped Home design, not a regression introduced by this task. Build/check/test green (12 pages, 0 errors, 37/37 tests). NOT live-browser-verified per this session's standing instruction.
- USER REQUEST 2026-07-05: user asked to also continue the Persistent Music Player plan (docs/superpowers/plans/2026-07-05-persistent-music-player.md) in this same session, interleaved with cross-scroll. Discovered its Task 3 (nowPlaying.ts engine, commit 98bcbe4) was already committed by the other concurrent session but never reviewed under this process — dispatched a review for it retroactively before proceeding to its Task 4.
- NOTE: shared .superpowers/sdd/task-N-*.md filenames collided with a third concurrent session's own decimal task numbering (its "Task 1.2" report was overwritten by mine). From this point forward, this plan's scratch files use the `xscroll-` prefix (e.g. xscroll-review-*.diff) to avoid further collisions; task-brief/report files for 0.1/0.2/1.1/1.2 remain under the original unprefixed names (already consumed, no longer at risk).

# Mobile-Responsive Navigation Progress Ledger

Plan: docs/superpowers/plans/2026-07-12-mobile-responsive-navigation.md
Spec: docs/superpowers/specs/2026-07-12-mobile-responsive-navigation-design.md
Branch: main (working directly on main per explicit user consent, no worktree)
Baseline commit: a71fb3c (prerequisite fix: exclude .worktrees/ from vitest)
Scratch file prefix: mobilenav- (avoids collision with other sessions' shared task-N-*.md files)

## Tasks
- Task 1: complete
- Task 2: complete
- Task 3: complete
- Task 4: complete
- Task 5: complete
- Task 6: complete
- Task 7: complete
- Task 8: complete
- Task 9: complete

## Log
- Task 1: complete (commit 6701711, review clean; note — plan doc's Step 4 prose said "10 assertions" but the code it prescribes has 8 it() blocks; harmless plan-doc inconsistency, not a code defect, no fix needed)
- Task 2: complete (commit b770042, review clean; sole-caller of initShowcaseMobile confirmed via grep, no leftover carousel/mode references)
- Task 3: complete (commit 523bfa3, review clean; mobile override fully deleted, falls through to base fixed-position rule; data-open-lane selector verified live against lanes.ts)
- Task 4: complete (commit 5a54c69, review clean/Approved on opus; all 13 spec steps verified, race-condition guards (state.closing, killTweensOf, await-completion, no-op guard) confirmed preserved byte-for-byte through the axis generalization. Inherited limitation noted but not a task-4 regression: laneMotion() is recomputed per-call in openLane/closeLane rather than snapshotted at open-time, so a viewport/reduced-motion change mid-open could open on one axis and close on another — verified pre-existing (isDesktopMotion() had the identical pattern before). Candidate for Task 9's breakpoint-crossing handling if ever observed live; not fixed here, out of scope.
- Task 5: complete (commit 5e0eb0b, review Approved; both edits byte-for-byte match the brief, correctly mobile-scoped. FOLLOW-UP (visual QA, not a defect): rotated .separator-arrows cluster (PanelSeparator.astro) swaps its box to 56x96 and rotates 90deg around center, but the child arrows' horizontal --tx fan-out was authored for the old 96-wide box — could end up off-center/clipped depending on ancestor overflow. Needs a real mobile-viewport check before considered visually final; not fixed since no browser testing was requested this session.)
- Task 6: complete (commit 11f239f, review Approved on opus; went beyond the guaranteed PanelMinimal email-wrap fix to also give PanelAbout a real mobile layout (order-based reordering, real content before decorative tagline) and remove 4 now-superseded !important rescues from global.css. All desktop-safety claims independently re-verified by the reviewer: removed selectors unused elsewhere, showcase.desktop.ts confirmed genuinely mobile-gated (parallax tween never runs on mobile), desktop CSS byte-identical, Astro scope-attribute specificity math confirmed the !important removal is safe. Hero/Photo/Card/Services reviewed and correctly left unchanged (no defects found). FOLLOW-UP (visual QA, not a defect): About panel's new decorative-tagline size/opacity/margin values are reasonable defaults per the implementer, flagged for a maintainer's live 375px pass to tune if desired.)
- Task 7: complete (commit f4a3a03, review clean; exact match to brief, selector/touch-target math independently verified against live file)
- Task 8: complete (commit a667ddc, review clean; box-model math independently verified for both min-height edits against Tailwind v4 Preflight border-box + no font-size override)
- Task 9: complete (commit 6cd4721, review clean; runGen/heroIntroPlayed guard interaction independently traced and confirmed safe for a third/Nth run() trigger. Optional "close open lane on resize" suggestion correctly skipped and verified justified — closeLane is not exported from lanes.ts, no way to call it externally without scope expansion. Minor note: implementer's report misattributed a brief quote that doesn't literally exist (harmless, doc-only, not a code issue).)

## ALL 9 TASKS COMPLETE — proceeding to final whole-branch review

## FINAL WHOLE-BRANCH REVIEW (base cad6d81 -> head 6cd4721, then fix 0db1272, re-review at head 0db1272)
- Round 1 (opus, full 9-task diff): Ready to merge "With fixes". Strong praise for the rotated-90deg metaphor holding structurally across tasks, viewport/slide()/arrow abstractions composing correctly, !important removal verified specificity-safe. 1 Important finding: Base.astro's Task 9 breakpoint re-init could leave a diagonal (both-axis) GSAP transform on [data-showcase-descent] after a phone rotation crossed the mobile/desktop boundary while a detail lane was open, because slide()'s per-axis tween never reset the OTHER axis and the new initLanes() closure's state.trackTween couldn't kill a tween from the OLD closure. 2 Minor (DRY: over-scroll handler inlined next-station lookup instead of using nextStationId(); latent '' vs null edge case in the same lookup). 1 Minor noted for the record only, not fixed (reduced-motion lane placement, pre-existing, out of scope).
- Fix (commit 0db1272, sonnet): root-cause fix inside slide() itself (not Base.astro/teardown, which the diagnosis showed was racy/insufficient) — explicitly pins the off-axis GSAP property (xPercent/yPercent) to 0 in all three tween-creation sites (panelsLayer .to(), lane fromTo() start+end, lane close .to()), replaces closure-scoped state.trackTween?.kill() with gsap.killTweensOf(panelsLayer) (kill-by-target, works across closure boundaries), consolidates both "next station" lookups onto nextStationId() with a proper .filter(Boolean) instead of `?? ''` coercion, removes the now-dead trackTween field from LaneState (confirmed zero remaining references). pnpm test lanes 14/14, pnpm check 0 errors, full pnpm test 48/48.
- Round 2 re-review (opus, focused on the fix): all three findings independently verified FIXED at the root cause in the live file; confirmed the "reset to 0" change cannot stomp any other transform consumer (grepped — [data-showcase-descent] has no other xPercent/yPercent writer; the horizontal scrub targets a different element, [data-showcase-track]). Zero new issues. READY TO MERGE: YES.

## PLAN COMPLETE — all 9 tasks + prerequisite infra fix + final review + fix + re-review, all clean.
Final HEAD: 0db1272

# Ambient Torroba Autoplay Progress Ledger

Plan: docs/superpowers/plans/2026-07-18-ambient-torroba-autoplay.md
Branch: main (in place)

## Tasks
- Task 1: complete
- Task 2: complete
- Task 3: complete
- Task 4: complete

## Log
- Task 1: complete (commits a35b010..fcdf89a, review clean; 2 Minor notes not fixed: toggleMute double-negation style, missing loadAmbient+close direct test)
- Task 2: complete (commits fcdf89a..2bd58b9, incl. e7b1654 impl + 2bd58b9 fix; review round 1 found 1 Important issue — implementer's commit e7b1654 bundled an unrelated, undisclosed edit to the Torroba album's bilingual bio copy in discography.ts (pre-existing uncommitted content from before this session) alongside the required `as Album` type-cast fix. Fixed in 2bd58b9 by reverting just the bio-copy lines, leaving the type-cast intact. Round 2 re-review confirmed the discography.ts diff nets to only the `as Album` cast, no other issues, Approved. `src/lib/ambientAutoplay.ts` itself was spec-compliant and clean from round 1.)
- Task 3: complete (commit dd774b9..359d731, `src/components/AmbientToggle.astro` new file only; review clean, byte-for-byte match to brief, transition:persist + fresh document.documentElement.lang copy resolution confirmed, visibility logic `!!s.track && !s.visible` confirmed, no other files touched, reduced-motion handled. Note: component not yet wired into a layout — that's Task 4.)
- Task 4: complete (commit c35be03..d1ce264, `src/layouts/Base.astro` only, 5-line wiring diff implemented directly by the controller (small/mechanical); review clean, exact match to brief placement. Environment note: `pnpm test`/`pnpm check`/`pnpm build` all fail in this environment via an unrelated `ERR_PNPM_IGNORED_BUILDS` pre-install gate (esbuild/sharp build-script approval never granted) — worked around by calling `./node_modules/.bin/vitest run`, `npx astro check`, `./node_modules/.bin/tsx scripts/check-i18n-keys.ts`, and `./node_modules/.bin/astro build` directly. All green: 52/52 unit tests, 0 type errors (1 pre-existing unrelated hint), i18n keys in sync, build produces 12 pages with 0 errors.)
- ALL 4 TASKS COMPLETE — proceeding to final whole-branch review.

## FINAL WHOLE-BRANCH REVIEW (base 344977b -> head 3478a4f, then fix 7e4e02b, re-review at head 7e4e02b)
- Round 1 (opus, full 11-commit branch diff): Needs fixes. 2 Important cross-task defects invisible to per-task review, both arising from the interaction between Task 1's engine and Task 3's toggle: (1) `close()` left `state.track` set, so `AmbientToggle`'s visibility predicate (`!!s.track && !s.visible`) read a just-closed bar as ambient state and reappeared with the equalizer animating — directly violating the spec's "closing the bar stops playback entirely, does not fall back to ambient" decision. (2) `load()` reset the `ambient` flag but not mute state, so muting during ambient playback silently carried into an explicit music-tab pick, with no mute control on the bottom bar to recover — a silent-playback trap. Also flagged 2 Minor (eq bars keep animating while the ambient track is paused at album end — same root cause as finding 1; unrelated pre-existing responsive-layout commit d279bff riding along on the branch, not part of this plan) and confirmed the two Task-1 Minors were fine to leave, though the missing loadAmbient+close test was now worth adding since it would have caught finding 1.
- Fix (commit 7e4e02b): `close()` now also sets `state.track = null` and `state.index = -1`. `load()` now also resets `audio.muted = false` and `state.muted = false`. Added 2 direct unit tests covering both. Full suite 54/54, astro check 0 errors.
- Round 2 re-review (opus): both fixes verified to resolve the defects at the root (engine level), confirmed no regression in `NowPlayingBar.astro`/`AlbumStoryPlayer.astro` consumers of `track`/`index`, confirmed the new tests are meaningful (not tautological) and would have failed pre-fix. The 2 Minor items (eq-bars-while-paused, bundled d279bff commit) remain open as non-blocking follow-ups. READY TO MERGE: YES.

## PLAN COMPLETE — all 4 tasks + final review + fix + re-review, all clean.
Final HEAD: 7e4e02b
Note: working in place on main (no feature branch) — no merge step needed; `finishing-a-development-branch` to be invoked next for any remaining wrap-up (e.g. surfacing the 2 open Minor follow-ups to the user).
